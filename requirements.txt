# Cloud Run / 本番ビルド用の依存リスト
# 開発時の .venv も同じバージョン揃え。
# ADK は Web UI 直接では使わないが boss_clone_lib/coordinator.py 等が import する。

google-adk==2.3.0
google-cloud-aiplatform==1.158.0
google-cloud-firestore==2.27.0
google-cloud-storage==3.12.0
google-genai==2.9.0
python-dotenv==1.2.2
streamlit==1.58.0
# 資料レビュー機能（Day 4 ファイル添付）
pypdf==6.14.0
python-docx==1.2.0
# Day 4 後半: アプリ内ベクトル検索（Vector Search からの移行、L-009）
# numpy 2.5+ は Python 3.12+ 必須。Dockerfile は 3.11-slim なので 2.4.6 を採用。
numpy==2.4.6
